# Windows Test Queues

Track Windows-only validation work that cannot be completed from remote Linux/SSH sessions.

Entry format:

- [ ] `<task>`. Source: `<request|application-todo.md|website-todo.md>`. Why Windows: `<reason>`. Validate: `<acceptance criteria>`. Added: `YYYY-MM-DD`.

## Pending

- [ ] Reproduce and fix Tauri app instability when clicking buttons. Source: `application-todo.md`. Why Windows: requires the real Tauri desktop runtime, native window process, and click-path correlation between frontend events and backend invokes. Validate: run `npm run tauri dev` on Windows, exercise the main dashboard, wizard, image manager, and settings button flows, capture the first crashing button path with frontend/backend logs, implement the fix, then confirm the same path is stable in both dev and packaged builds. Added: `2026-04-08`.
- [ ] Validate app icon behavior in Tauri dev and packaged builds. Source: `application-todo.md`. Why Windows: requires native Tauri window rendering and packaged bundle inspection. Validate: run `npm run tauri dev` and confirm whether the window/taskbar icon is missing only in dev, then build a packaged app and confirm the packaged executable/MSI shows the configured BitOSDT icon in the titlebar, taskbar, and shell surfaces. Added: `2026-04-08`.
- [ ] Validate WinPE shell startup no longer hits `Taskbar class not registered` and that the BitOSDT UI opens fullscreen. Source: `application-todo.md`. Why Windows: requires a real WinPE boot with HTA/native shell startup, WinPE optional components, and actual shell/window behavior that cannot be reproduced from Linux. Validate: boot generated WinPE media on Windows-capable hardware or VM, capture `X:\BitOSDT\Logs\startnet.log` and `X:\BitOSDT\Logs\shell-launch.log`, confirm no `Taskbar class not registered` error appears, confirm the BitOSDT deployment UI launches and remains usable, and confirm the shell covers the full display through the initial startup sequence. Added: `2026-04-09`.
- [ ] Validate implemented "Write to USB" flow for package/image manage views, including drive picker that blocks `C:\`, typed destructive wipe confirmation (`WIPE DISK N`), raw ISO write, and provisioning payload copy (`Provisioning\<Profile>`, plus root `.ppkg` sidecar layout). Source: `application-todo.md`. Why Windows: requires real removable-drive behavior and admin-level disk operations. Validate: perform end-to-end writes on test USB drives and confirm resulting media boots/loads payloads with no extra operator steps. Added: `2026-03-12`.
- [ ] Validate existing-image edit flow loads saved wizard state without hanging on `Loading image profile...` and still honors save mode (`Overwrite existing image` vs `Save as new copy`). Source: `application-todo.md`. Why Windows: final workflow validation must confirm real Tauri desktop-runtime behavior for preload, edit-state retrieval, and subsequent image-save/build flows. Validate: open an existing image from Manage Images, confirm the wizard does not remain stuck on `Loading image profile...`, confirm saved values prefill correctly for output/domain/apps/autopilot, confirm the slow-load warning and exit action appear if payload retrieval is artificially delayed or fails, then run both save modes and confirm overwrite updates the same profile while copy creates a new profile. Added: `2026-04-09`.
- [ ] Validate implemented PXE hardening on real WDS path: lightweight runtime startup fallback and full-ISO install image resolution order (local -> UNC -> HTTP). Source: `application-todo.md`. Why Windows: PXE/WDS boot and WinPE networking cannot be validated on Linux. Validate: boot via PXE, confirm BitOSDT starts without runtime exit loop, and verify deployment can resolve install image from configured local/UNC/HTTP fallback or emits new detailed search diagnostics. Added: `2026-03-12`.
- [ ] Validate WDS/PXE prepared-payload enforcement and runtime diagnostics on real WinPE boot. Source: `user request`. Why Windows: requires actual PXE/WDS boot behavior, hosted SMB/HTTP payloads, and confirmation that offline customizations survive only when the exported prepared WIM is served. Validate: boot one PXE build with the exported `C:\BitOSDT\WDS\install.wim` hosted at the configured runtime path and confirm OOBE users, Autopilot, and other offline config apply; then boot another PXE build against an original or mismatched WIM and confirm BitOSDT fails before apply with the new size/hash mismatch guidance while UNC-only and HTTP-only errors still name only the configured source plus local search results. Added: `2026-04-01`.
- [ ] Validate implemented WDS sign-in safety defaults, staged image-preparation progress, and WinPE fullscreen enforcement on real hardware. Source: `application-todo.md`. Why Windows: requires actual WDS/PXE builds, WinPE shell behavior, and deployed Windows sign-in paths that cannot be exercised from Linux. Validate: build a WDS/PXE image from Microsoft CDN Windows 11 25H2, confirm progress advances through `prepare-*` stages without stale ESD-conversion warnings after conversion completes, confirm HTA/native WinPE UI covers the full display, confirm default deployments reach usable OOBE/sign-in, and confirm `Skip User OOBE` plus a configured local administrator still allows sign-in. Added: `2026-04-01`.
